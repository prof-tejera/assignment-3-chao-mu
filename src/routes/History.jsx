// Ours - Styles
import styles from "./History.module.css";

const History = () => {
  return (
    <section className={styles["history"]}>
      <div className={styles["workout-history"]}></div>
    </section>
  );
};

export default History;
